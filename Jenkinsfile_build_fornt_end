pipeline{
    agent any
     environment {
        
    } 
    stages{
        stage("Build the code"){
            steps{
                script{
                    echo "11111" >> index.html
                    tar -zcvf 11111.tar.gz index.html
                     #curl -v -u admin:admin123 --upload-file index.html http://localhost:8081/repository/artifacts/11111/index.html
                } 
            }
        }
    }
    post{
        always {  
            cleanWs()
        }
    }
}
def randomIDGet = sh(script: 'uuid', returnStdout: true)

pipeline{
    agent any
    stages{
        stage("Build the code"){
            steps{
                script{
                    echo randomIDGet
                } 
            }
        }
      
    }
    post{
        always {  
            cleanWs()
        }
    }
}
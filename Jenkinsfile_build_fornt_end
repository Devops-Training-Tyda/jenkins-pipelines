pipeline{
    agent any
    stages{
        stage("Build the code"){
            steps {  
                sh """
                    myVar="test";
                    echo "${myvar}" > index.html
                    ls -l
                """
            }
        }
    }
    post{
        always {  
            cleanWs()
        }
    }
}
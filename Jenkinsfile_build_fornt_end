pipeline{
    agent any
     environment {
        randomID = "111111"
    } 
    stages{
        stage("Build the code"){
            steps {  
                script{
                    def randomID = sh(script: 'uuid', returnStdout: true)
                    println(randomID)
                } 
            }
        }
    }
    post{
        always {  
            cleanWs()
        }
    }
}